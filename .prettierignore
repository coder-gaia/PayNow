node_modules
dist
build
coverage
.turbo
.next
pnpm-lock.yaml
apps/api/prisma/migrations

# Gerado pelo Next a cada build. Nao esta no repositorio, entao o CI nunca o ve:
# sem esta linha, `format:check` acusa localmente e passa no CI, que e a pior
# combinacao possivel.
next-env.d.ts

# Saida do Playwright. Ignorada pelo git, entao o CI nunca a ve: sem esta linha
# o `format:check` acusa localmente e passa la, que e a pior combinacao.
test-results/
playwright-report/
